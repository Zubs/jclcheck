{
  "name": "jcl-checker-backend",
  "version": "1.0.0",
  "description": "SaaS-grade JCL validation engine (lexer, parser, AST, rules) with an HTTP API.",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "test": "node test/run.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2"
  }
}
